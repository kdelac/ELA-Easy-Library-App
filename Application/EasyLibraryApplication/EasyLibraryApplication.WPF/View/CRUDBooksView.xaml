<Page x:Class="EasyLibraryApplication.WPF.View.CRUDBooksView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:EasyLibraryApplication.WPF.View"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="600"
      Title="CRUDBooksView"
      Background="#2488e0">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="11*"></RowDefinition>
            <RowDefinition Height="146*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="53*"></ColumnDefinition>
            <ColumnDefinition Width="100*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="1" Name="StackPanelInfo" Grid.ColumnSpan="2" Margin="0,1.2,371.6,0.4">
            <TextBlock Margin="0,0,0,5" Text="Naziv knjige:"></TextBlock>
            <TextBox Text="{Binding SelectedItem.Title, Mode=TwoWay}"></TextBox>
            <TextBlock Margin="0,10,0,5" Text="ISBN:"></TextBlock>
            <TextBox Text="{Binding SelectedItem.ISBN, Mode=TwoWay}"></TextBox>
            <TextBlock Margin="0,10,0,5" Text="Broj stranica:"></TextBlock>
            <TextBox Text="{Binding SelectedItem.Datum, Mode=TwoWay}"></TextBox>
            <TextBlock Margin="0,10,0,5" Text="Opmerking:"></TextBlock>
            <TextBox Text="{Binding SelectedItem.Opmerking, Mode=TwoWay}"></TextBox>
            <TextBlock Margin="0,10,0,5" Text="Credit:"></TextBlock>
            <TextBox Text="{Binding SelectedItem.Credit, Mode=TwoWay}"></TextBox>
            <UniformGrid Margin="0,10,0,5" Rows="1" >
                <Button Margin="0,0,10,0" Content="Refresh" Command="{Binding RefreshEvent}"></Button>
                <Button Margin="10,0,10,0" Content="Delete" Name="ButtonDelete" Click="ButtonDelete_Click"></Button>
                <Button Margin="10,0,0,0" Content="{Binding ButtonAddContent}" Command="{Binding AddEvent}"></Button>
            </UniformGrid>
            <Button Content="Save" Command="{Binding SaveEvent}"></Button>

        </StackPanel>


        <Button Content="&lt;--" HorizontalAlignment="Left" Padding="10,2,10,2"  VerticalAlignment="Top" Command="{Binding PreviousEvent}" Height="22" Width="40" />
        <Button Content="--&gt;" HorizontalAlignment="Right" Padding="10,2,10,2" VerticalAlignment="Top" Command="{Binding NextEvent}" Grid.ColumnSpan="2" Height="22" Margin="0,0,371.6,0" Width="40" />

        <DataGrid x:Name="schoolresultatenDataGrid"  Margin="25.4,0,-0.4,0.4" Grid.Column="1" Grid.RowSpan="2" RowDetailsVisibilityMode="VisibleWhenSelected" EnableRowVirtualization="True" AutoGenerateColumns="False" ItemsSource="{Binding Collection.View}" SelectedItem="{Binding SelectedItem}">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="naamVakColumn" Binding="{Binding NaamVak}" Header="Naam Vak" Width="Auto"/>
                <DataGridTextColumn x:Name="scoreColumn" Binding="{Binding Score}" Header="Score" Width="Auto"/>
                <DataGridTextColumn x:Name="datumColumn" Binding="{Binding Datum}" Header="Datum" Width="Auto"/>
                <DataGridTextColumn x:Name="opmerkingColumn" Binding="{Binding Opmerking}" Header="Opmerking" Width="Auto"/>
                <DataGridTextColumn x:Name="creditColumn" Binding="{Binding Credit}" Header="Credit" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
